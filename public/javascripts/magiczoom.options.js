MagicZoom.options = {
    // Width of zoom window (px)
    'zoom-height' : '300',
    // Position of zoom window (left / right / top / bottom / inner /custom)
    'zoom-position' : 'right',
    // Distance from small image to zoom window (px) 
    'zoom-distance' : 15,
    // Opacity of hovered area (0-100)
    'opacity' : 50,
    // Add opacity outside mouse box (true / false)
    'opacity-reverse' : 'false',
    // Enable smooth zoom movement (true / false)
    'smoothing' : 'true',
    // Speed of smoothing (1-99)
    'smoothing-speed' : 40,
    // Frames per second for zoom effect 
    'fps' : 25,
    // Show the title of the image in the zoom window (top / bottom / false)
    'show-title' : 'top',
    // Zoom window fade effect (true / false)
    'zoom-fade' : 'false',
    // Zoom window fade-in speed (ms) 
    'zoom-fade-in-speed' : 400,
    // Zoom window fade-out speed (ms) 
    'zoom-fade-out-speed' : 200,
    // Multiple images change on click/mouseover (click / mouseover)
    'thumb-change' : 'click',
    //  Delay before switching thumbnails (ms)
    'selectors-effect' : 200,
    // Speed of dissolve/fade effect (ms) 
    'selectors-effect-speed' : 400,
    // Multiple images, preload small images (true / false)
    'preload-selectors-small' : 'true',
    // Multiple images, preload large images (true / false)
    'preload-selectors-big' : 'false',
    // Click to fetch the large image (true / false)
    'click-to-initialize' : 'false',
    // Click to show the zoom (true / false)
    'click-to-activate' : 'false',
    // Loading message (true / false)
    'show-loading' : 'true',
    // Loading message text 
    'loading-msg' : 'Loading zoom...',
    // Loading message opacity (0-100)
    'loading-opacity' : 75,
    // Loading message X-axis position, -1 is center 
    'loading-position-x' : '-1',
    // Loading message Y-axis position, -1 is center 
    'loading-position-y' : '-1',
    // Click and drag to move the zoom (true / false)
    'drag-mode' : 'false',
    // Click to move the zoom (in drag mode) (true / false)
    'move-on-click' : 'true',
    // Remember position of zoom for multiple images and drag mode (true / false)
    'preserve-position' : 'false',
    // Initial X-axis position for drag mode, -1 is center 
    'x' : '-1',
    // Initial Y-axis position for drag mode, -1 is center 
    'y' : '-1',
    // Make zoom window always visible. (Automatically true in drag-mode.) (true / false)
    'always-show-zoom' : 'false',
    // Resize zoom window if big image is smaller than zoom window (true / false)
    'fit-zoom-window' : 'true',
    // Show the entire large image on hover (true / false)
    'entire-image' : 'false'
}